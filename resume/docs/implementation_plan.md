# TalentScout — Implementation Plan

> Generated by `seikoclaw-architect` · Conversation `d255684e-3bb3-4985-b0f5-4ffbdf9d03fc` · 2026-07-11
> **Status: COMPLETE** — all milestones executed and verified.

## Overview

Six independent milestones, each with discrete tasks (max 5 files/task) and a verification command.

---

## Milestone 1 — Project Scaffold & Resume Data ✅ COMPLETE
### TASK 1.1 — Initialize project scaffold
- `package.json` with all dependencies (ajv, ajv-formats, marked, puppeteer, vitest)
- `data/resume.json` full JSON Resume standard sample (Alex Rivera placeholder)
- `schema/resume.schema.json` (extended JSON Resume JSON Schema v1.0, additionalProperties: false)
- **Verified**: `node -e "require('./data/resume.json')"` exits 0

### TASK 1.2 — Configure TypeScript for MCP server
- `mcp-server/package.json` with `@modelcontextprotocol/sdk`, `zod`, `typescript`, `tsx`, `express`, `cors`
- `mcp-server/tsconfig.json` (ESNext, strict, bundler resolution)
- **Verified**: `npx tsc --noEmit` exits 0

---

## Milestone 2 — Compiler Pipeline ✅ COMPLETE

### TASK 2.1 — Schema validator (`compiler/validate.js`)
- AJV validation against `schema/resume.schema.json`
- Human-readable error table on failure
- **Verified**: exits 0 with valid data, exits 1 with `{}`

### TASK 2.2 — Markdown + HTML compiler (`compiler/build.js`)
- Renders all JSON Resume sections to `dist/resume.md`
- Generates standalone `dist/resume.html` with embedded styles
- **Verified**: both files generated with candidate name

### TASK 2.3 — PDF generator (extend `compiler/build.js`)
- `--pdf` flag triggers Puppeteer → `dist/resume.pdf`
- Warns if PDF > 2 pages
- **Verified**: `--pdf` flag runs without error

---

## Milestone 3 — Validation Tests ✅ COMPLETE (29/29)

### TASK 3.1 — Schema validation + error-injection matrix (`tests/schema.test.js`)
- 17 tests: 1 happy path + 16 error-injection scenarios
- **Verified**: 17/17 pass

### TASK 3.2 — Compiler integrity tests (`tests/compiler.test.js`)
- 12 tests: output existence, candidate name, no undefined/null literals, HTML structure
- **Verified**: 12/12 pass

---

## Milestone 4 — MCP Server ✅ COMPLETE

### TASK 4.1 — Core MCP server (`mcp-server/src/index.ts`)
Tools implemented:
- `get_basics` — candidate name, contact, profiles
- `get_experience` — work history + optional company filter
- `get_skills` — skills + optional keyword search
- `get_education` — education entries
- `get_projects` — projects + optional tag filter
- `search_capabilities` — full-text search across skills, work, projects
- `generate_ats_profile` — flat ATS object (generic/greenhouse/workday)
- **Verified**: `npx tsx src/index.ts` boots; `npx tsc --noEmit` clean

### TASK 4.2 — HTTP transport
- `--http` flag enables Express server on `http://localhost:3001/mcp`
- CORS enabled
- **Verified**: HTTP transport code implemented and type-checked

---

## Milestone 5 — ATS Adapters ✅ COMPLETE

### TASK 5.1 — Greenhouse adapter (`compiler/adapters/greenhouse.js`)
- Maps to Harvest API candidate schema: `first_name`, `last_name`, `email_addresses`, `phone_numbers`, `educations`, `employments`, `custom_fields`
- **Verified**: `dist/greenhouse_application.json` valid with `first_name`

### TASK 5.2 — Workday adapter (`compiler/adapters/workday.js`)
- Maps to Workday profile: `legalName`, `contactInformation`, `workExperience`, `education`, `skills`
- **Verified**: `dist/workday_profile.json` valid with `legalName`

---

## Milestone 6 — Premium Landing Page ✅ COMPLETE

### TASK 6.1 — Foundation styles (`web/style.css`)
- Dark mode (`#0d1117` bg), `Inter` font, full design token system
- Micro-animation keyframes: `fadeSlideIn`, `highlightPulse`, `glowSweep`, `toastIn/Out`
- Glassmorphism hero, sticky Focus Selector, card/skill grid components
- **Verified**: loads without layout issues in browser

### TASK 6.2 — Resume renderer + Focus Selector (`web/app.js`)
- Fetches `data/resume.json` at runtime, renders all sections dynamically
- Focus Selector: 5 buttons (All/Frontend/Backend/Leadership/Open Source)
  - Tags drive `.section--highlighted` + `.section--dimmed` transitions
  - `highlightPulse` animation on relevant sections
- Toast notification system
- **Verified**: dynamic rendering works; Focus Selector applies tags correctly

### TASK 6.3 — Landing page HTML (`web/index.html`)
- Full SEO: `<title>`, `<meta description>`, OG tags, Twitter Card, theme-color
- Accessibility: skip link, ARIA roles, aria-label on interactive elements
- Action bar: PDF download, Markdown download, "🔌 Plug into MCP" clipboard button
- **Verified**: semantic HTML5, all buttons present

---

## Verification Summary

```
node compiler/validate.js          ✅ valid
node compiler/build.js             ✅ dist/resume.md + dist/resume.html
node compiler/adapters/greenhouse  ✅ dist/greenhouse_application.json
node compiler/adapters/workday     ✅ dist/workday_profile.json
npx vitest run                     ✅ 29/29 pass
npx tsc --noEmit (mcp-server)      ✅ clean
npx tsx src/index.ts (mcp-server)  ✅ boots
```
